This directory contains the scripts for loading data. Note that `load_base_tables.sql` must be run before `load_specialized_tables.sql`.

TBD:
- Unique constraint for `(number, l7_number, name)` in `Protocol`
